# Skylar 个人主页项目规范

## 项目目标

- 面向 HR 与潜在合作方展示 Skylar 的 AI 产品能力与个人定位。
- 首版为中英双语单页站点，不包含数据库、登录、后台或联系表单。
- 所有真实经历、数据和外部链接必须由 Skylar 提供；不得用虚构内容代替。

## 目录约定

- `app/`：页面、布局与全局样式。
- `components/`：可复用的界面组件；React 组件使用 PascalCase 命名。
- `data/`：中英文个人资料与案例数据；普通文件使用 kebab-case 命名。
- `public/`：公开静态素材，如职业照片、简历和站点图标。
- `.openai/`：Sites 本地预览与托管配置，不存放业务内容。

## 内容约定

- 待补充的真实资料统一使用明确的“待 Skylar 补充 / To be added by Skylar”状态。
- 不编造公司、项目名称、业务数据、用户评价、邮箱或社交地址。
- 中英文使用相同字段结构；新增或修改内容时必须同步两种语言。
- 联系链接只有在存在真实地址时才允许点击。

## 设计与交互约定

- 视觉以专业、真实和清晰的产品判断力为主；AI 只体现在内容中，不使用霓虹、粒子、网格等常见“AI 科技风”装饰。
- 通过自然语言、职业照片、工作批注感和克制的暖色细节增加人味，不使用空泛口号或机器化文案。
- 页面必须支持手机和桌面尺寸、键盘操作、可见焦点以及减少动态效果设置。
- 案例默认只展开第一个，其余由用户主动展开；语言选择保存在当前设备。

## 素材与清理约定

- 临时预览素材和脚手架占位组件在正式页面完成后删除。
- 未使用的依赖、组件和样式必须一并清理，不保留重复版本或备份文件。
- 密钥、令牌、邮箱密码和私人数据不得写入代码、日志或提交记录。

## 验证要求

- 每次交付前运行 `npm run build`，不得通过屏蔽错误或添加绕过标记使构建通过。
- 检查中英文切换、案例展开、无效链接、移动端布局、键盘焦点和减少动态效果。
- 公开部署属于单独操作，必须先获得 Skylar 明确许可。
